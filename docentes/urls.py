from django.urls import path
from . import views

app_name = 'docentes'

urlpatterns = [
    # URLs básicas para docentes
    # path('', views.docente_list, name='docente_list'),
    # path('<int:pk>/', views.docente_detail, name='docente_detail'),
] 